/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Si tu héberges sur un sous-chemin (ex: GitHub Pages), décommente et adapte :
  // basePath: '/generation-7000',
};

module.exports = nextConfig;
